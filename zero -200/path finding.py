import os
#select the directory the want is lisout
directory_path = '/'
#use the directory module to list the content
contents = os.listdir(directory_path)
#print the contents of the directory
print(contents)